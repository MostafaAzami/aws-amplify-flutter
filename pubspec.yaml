
name: aws_amplify_flutter_web
description: A Flutter Web app integrated with AWS Amplify
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  amplify_flutter: ^1.0.0
  amplify_auth_cognito: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
